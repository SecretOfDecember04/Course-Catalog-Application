<%= form_with(model: course_taken) do |form| %>
  <% if course_taken.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course_taken.errors.count, "error") %> prohibited this course_taken from being saved:</h2>

      <ul>
        <% course_taken.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :grader, style: "display: block" %>
    <%= form.text_field :grader %>
  </div>

  <div>
    <%= form.label :course, style: "display: block" %>
    <%= form.text_field :course %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
