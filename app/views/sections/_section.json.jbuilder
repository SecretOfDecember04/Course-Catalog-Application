json.extract! section, :id, :section_number, :course, :term, :campus, :instructor, :days_times, :instruction_mode, :graders_required, :graders, :created_at, :updated_at
json.url section_url(section, format: :json)
